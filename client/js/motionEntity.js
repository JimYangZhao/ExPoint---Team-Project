//An entity that can be in motion. Inherients from entity.js

motionEntity = function(x,y,id,velX,velY){

    self = entity(x,y,id);

    self.velX = velX;
    self.velY = velY;

    function update(){
        
    }
};